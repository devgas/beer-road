{
  "name": "beer-road",
  "version": "1.0.0",
  "private": true,
  "description": "Beer Road Save - Discover, save, and plan your perfect brewery road trip",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "install:all": "npm install && cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "seed:sqlite": "node server/scripts/seed-sqlite.js",
    "vercel-build": "cd server && npm install && node scripts/seed-sqlite.js && cd ../client && npm install && npm run build"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.11.0",
    "serverless-http": "^3.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
